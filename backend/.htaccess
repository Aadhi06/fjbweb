<IfModule mod_rewrite.c>
    RewriteEngine On

    # Hostinger: use when subdomain document root is the Laravel folder (api/)
    # and NOT api/public. Sends all requests into public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
